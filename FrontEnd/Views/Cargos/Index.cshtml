@model IEnumerable<FrontEnd.Models.CargosViewModel>

@{
    ViewData["Title"] = "Cargos";
}
<h2><span style="color: darkgrey; font-weight: bold;">Lista de cargos</span></h2>
<p>
    <a asp-action="Create" class="btn btn-outline-primary btn-sm">Insertar nuevo Cargo</a>
</p>
<table id="tablaCargos" class="table table-striped table-bordered table-hover" style="width:100%">
    <thead class="table-primary">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdCargo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NombreCargo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdCargo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NombreCargo)
            </td>
            <td>
                 <a asp-action="Details" asp-route-id="@item.IdCargo" class="btn btn-outline-info btn-sm"><i class="fa fa-eye" aria-hidden="true" style="font-size: 20px;"></i></a>
                 <a asp-action="Edit" asp-route-id="@item.IdCargo" class="btn btn-outline-warning btn-sm"><i class="fa fa-pencil" aria-hidden="true" style="font-size: 20px;"></i></a>
                 <a asp-action="Delete" asp-route-id="@item.IdCargo" class="btn btn-outline-danger btn-sm"><i class="fa fa-trash" aria-hidden="true" style="font-size: 20px;"></i></a>
            </td>
        </tr>
}
    </tbody>
</table>
@section scripts{


    <script>

        $(document).ready(function () {
            $('#tablaCargos').DataTable({
                "language": {
                    "url": "//cdn.datatables.net/plug-ins/1.13.2/i18n/es-ES.json"
                },
                dom: 'Bfrtip',
                buttons: [
                    '', '', ''


                    , {
                        extend: 'pdfHtml5', className: "btn btn-outline-secondary btn-sm",
                        exportOptions: {
                            columns: [0, 1]
                        }
                    }

                    , {
                        extend: 'excelHtml5', className: "btn btn-outline-secondary btn-sm",
                        exportOptions: {
                            columns: [0, 1]
                        }
                    }
                    , {
                        extend: 'csvHtml5', className: "btn btn-outline-secondary btn-sm",
                        exportOptions: {
                            columns: [0, 1]
                        }
                    }
                    , {
                        extend: 'print', className: "btn btn-outline-secondary btn-sm",
                        exportOptions: {
                            columns: [0, 1]
                        }
                    }
                ],
            });
        });

    </script>


}